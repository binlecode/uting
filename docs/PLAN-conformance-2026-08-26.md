# PLAN — conformance audit (uting) · 2026-08-26

Scope: all eight scripts in `shell/`   ·   Functions graphed: 194   ·   Prior report folded: none
Tree state: the audit's first pass read a worktree where the `--auth` unit of work was still in
flight. That work has since **landed** (`51a9415` → docs resync `d7584de`, version `0.3.6`), its
`PLAN-` is deleted, and every row below was **re-verified against the post-landing worktree on
2026-08-26** — two findings shrank because the landing fixed half of them, and the affected rows
say so. Today's dirty tree is a `bili-search` unit of work plus audit-skill edits; none of it
overlaps a row here, and this plan must not be landed inside it.

## Clean classes (swept, nothing found)

| rule | evidence |
|---|---|
| R2 redundant state | read all 15 `CURRENT_PLAY_*` / `CURRENT_QUEUE_*` mutation sites (`shell/uting:714-747`, `1695-1709`, `2792-2822`). `CURRENT_PLAY_TITLE` vs `CURRENT_PLAY_MPV_TITLE` have genuinely different lifecycles (row metadata vs mpv's own report, used for queue-advance detection) — not one concept under two names. |
| R3 layer back-edge | `grep 'yt-dlp\|mpv \|--input-ipc-server' shell/uting` · `grep 'yt-dlp\|cookies-from-browser' shell/ut-play` · `grep 'mpv \|players/' shell/*-search shell/*-resolve` · `grep 'yt-dlp\|mpv ' shell/ut-playlist shell/ut-history` — **every hit is a comment or a doc pointer.** Read all 30. No primitive call crosses a layer. |
| R7 optimistic state | `shell/uting:797` sets `TTY_ECHO_OFF=1` only on `stty &&`. `shell/uting:2790` writes no `CURRENT_PLAY_*` until `rc==0` **and** `id`/`sock` are present in the envelope. `ut-play:1273` writes the queue before the child launches, deliberately and documented. |
| R10 dead code | one DEAD candidate, `child_signal` (`shell/ut-play:925`), called from the trap strings at `948`/`949` — the exact false positive the skill warns about. Zero real dead functions. |
| R12 terminal ownership | no `printf`/`echo` in `shell/uting` interpolates `R_TITLE`/`R_CHAN`/`CURRENT_PLAY_TITLE` outside the width layer. Echo and cursor are taken from a `stty -g` snapshot (`796`) and restored from the single trap at `1821` (`EXIT INT TERM HUP`). |
| R8 single-line `-j` | ran `ut-play --status -j`, `--stop --all -j`, `ut-playlist --ls -j`, `--show nope -j`, `ut-history --ls -j` under a disposable `UT_STATE_DIR`: **stdout is exactly 1 line in all five**, incl. the error envelope. Gates verified: `yt-search --detach`→1, `ut-play "a query"`→1, `ut-play -- "a query"`→1, `ut-play --info`→1, `ut-play` (no args)→1, `uting </dev/null`→1, `--stop --all` idempotent 0/0. |

## Inventory (read-confirmed, re-verified post-landing)

| rule | file:line | what | fix |
|---|---|---|---|
| **R6/R8** | `shell/ut-play:1928` | `lock_player_state "$id"` is a **bare command** in `do_set_volume`, where the four sibling call sites (`405`, `1298`, `1320`, `1341`) all carry `\|\| true`. Under `set -euo pipefail` a lock-spin timeout **aborts the script** — verified: `/bin/bash -c 'set -euo pipefail; f(){ return 1; }; echo before; f; echo after'` prints `before`, exits 1. The IPC at `1920` has **already changed the volume on mpv** by then. Net effect: volume changed, `.volume` not persisted, **no `-j` envelope emitted at all** (`ut-play` installs no EXIT trap), exit **1** — a code the taxonomy reserves for usage/validation. | `\|\| true` at `1928`. The posture question is **already decided**: the lock's own header comment (`ut-play:1164-1166`) declares *"on timeout the caller proceeds unlocked, same best-effort risk these patches already tolerated"* — the four siblings honor that documented intent and `1928` breaks it. One line, no new prose beyond the §9 sentence in the done_when. |
| **R4** | `shell/ut-play:260` · `shell/yt-resolve:280` · `shell/bili-resolve:273` | `normalize_playback_mode` is **byte-identical in three files** (md5-equal bodies, re-checked) **and spans the player/engine boundary** — R4's stated boundary-leak case, not the standalone-executable carve-out. The alias table (`ba bv fst asc waves wave`) is playback vocabulary. Worse: the player normalizes at flag-parse (`ut-play:2291`) and sends the **canonical** mode to the engine (`ut-play:335`), so the engines' alias half is **unreachable through the player** — it serves only someone typing `yt-resolve -f ba` by hand. Adding a sixth mode today means three files in lockstep, and a drift fails as a `die` from the wrong layer. | declare in `AS-BUILT-contract.md` §3/§6 that **the engine seam carries canonical modes only** (`audio video fast ascii viz`). `ut-play` keeps the one alias table. Each resolver's `-f` gate becomes `validate_enum -f "$OPTARG" audio video fast ascii viz` — a primitive both already have. One alias table, and engine #3 inherits it. |
| **R11** | `docs/AS-BUILT-contract.md:637,642` | *Half of this landed with `--auth` (`d7584de`): §5's cookie row now states the engine-name-uppercase rule and "只有引擎读它" — the original false universality claim is gone.* What survives, re-read against the worktree: **(a)** the `BILI_*` knob family is enumerated **nowhere** — `docs/` names only `BILI_UA` (`AS-BUILT-engine.md:107`), while the code has **six** real knobs (`BILI_COOKIE_BROWSER` `BILI_AUDIO_FORMAT` `BILI_VIDEO_FORMAT` `BILI_VIDEO_FORMAT_FAST` `BILI_UA` `BILI_BUVID` — all `:=`/`:-` reads, `bili-resolve:58-72`, `bili-search:71,89`); **(b)** `:642` still says `YT_AUDIO_FORMAT`/`YT_VIDEO_FORMAT*` *"住在每个 `<engine>-resolve` 里"* — false for bili, whose spellings are `BILI_*`. NOT knobs, despite the plan's first pass listing them: `BILI_COOKIE_ARGS` (`bili-resolve:71`) and `YT_COOKIE_ARGS` (`yt-resolve:63`) are **internal arrays** built *from* the cookie decision — plain `=()` assignments that overwrite any env value (and bash cannot export arrays). They move to the R9 row below. | list the six `BILI_*` knobs in §5 and make the format row point at the prefix convention instead of naming the yt spelling as if it were universal. |
| **R11** | `docs/AS-BUILT-contract.md:683` | *Half of this landed too:* §6 item 3 (`:694`) now names `FOO_COOKIE_BROWSER` and the uppercase rule — **for the cookie knob only**. Items 4–5 and the closing say nothing about a new engine's *other* tuning: a `foo` author still has no way to know whether their format knob is `YT_`, `UT_` or `FOO_`. | add the general statement to §6: an engine's own tuning reads `FOO_*`; `UT_*` is suite-wide (`UT_STATE_DIR`, `UT_DEFAULT_ENGINE`, `UT_HISTORY`); `YT_*` is the yt engine's own prefix plus the frozen legacy suite-wide names, and is **not a template**. §5's per-engine rows point at it. |
| **R11** | `docs/AS-BUILT-contract.md:614-666` | §5's enumeration is short of the code in **two** places (not four — the first pass misclassified two internal globals as knobs): `YT_AMBIG_WIDE` (`shell/uting:1065`, real user knob, in `uting --help:269` and `AS-BUILT-tui.md:368-393`) and `YT_DETACHED_LOG` (`ut-play:1019`, internal, set by the player for its child). Dropped from the first pass's list: `YT_COOKIE_ARGS` (internal array, see the R4/R9 rows) and `UT_HISTORY_BIN` (`ut-play:1057`, `uting:71` — a **plain assignment** that overwrites any env value; internal plumbing like `UT_PLAY`/`UT_PLAYLIST`, not a knob). | add `YT_AMBIG_WIDE` to §5's user knobs and `YT_DETACHED_LOG` to its `内部` block, in the same §5 edit as the row above. |
| **R11** | `docs/AS-BUILT-contract.md:614` | §5's flag enumeration omits **`-p`** (`shell/uting:334`, rows per page, in `uting --help`) **and `--auth`** (landed since the first pass; documented in §1.3/§3/§6 but absent from this list). | add both, same §5 edit. |
| **R9** | `shell/ut-play:67,82,84` · `shell/yt-resolve:63` · `shell/bili-resolve:71` | `YT_MPV_AUDIO_OPTIONS` / `YT_MPV_VIDEO_OPTIONS` / `YT_MPV_ASCII_OPTIONS`, plus `YT_COOKIE_ARGS` / `BILI_COOKIE_ARGS` (reclassified here from the first pass's R11 rows), are **plain array assignments** — an inherited env value is silently overwritten, yet they wear the prefix §5 defines as meaning "environment variable, set once". Contrast their neighbours `YT_ASCII_VO` (`ut-play:64`) and `YT_MPV_INPUT_CONF`, which **are** `:=` knobs and **are** in §5. | rename to the internal shape (`MPV_AUDIO_OPTIONS`, `COOKIE_ARGS`, …) so the prefix stops promising a knob. They are argv we build, not tuning we accept — and arrays could never have been env knobs anyway. |
| **R1/R10** | `shell/uting:415,436,443,446` | `C_YELLOW` — **four write sites, zero read sites** (re-confirmed across `shell/`, `tests/`, `docs/`; `uting:2732` and `402-406` are comments). Retained deliberately: `uting:406` argues a stale read of it is harmless, and `RESEARCH-oss-landscape.md:202` / `AS-BUILT-tui.md:295,301` / `AS-BUILT-verification.md:216` all record it as the one intentional SC2034. But there is **no read to be stale** — the rationale defends a re-introduction that has not happened, and costs four assignments plus three doc paragraphs to preserve. | pick one and stop paying twice: delete the four assignments **and** the three doc mentions (its per-theme hexes are in git, same as `C_GREEN`'s), or keep it and drop the tracked-exception prose. Recommend deleting — `C_GREEN` was already deleted outright for the same reason, and the asymmetry is what makes this a recurring conversation. |
| **R5** | `shell/uting:1208,1225,1232,1464` | four bare `((var += …))` **statements** under `set -euo pipefail`, which CLAUDE.md forbids absolutely ("Never `((n += w))` as a statement; write `n=$((n + w))`"). Traced all four: `iw` ≥ 1 (`1 + DISP_W`), `sep_w` ≥ 2 (`${HINT_SEP:-"  "}`, and every real assignment is `" $GL_SEP "` — `uting:2136,2237,2546,2653`), `cur_w += w_len + 1` ≥ 1. **All provably non-zero today — latent, not live.** But the safety is an argument about `print_hints`' callers, and it is re-derived by every reader. | mechanical rewrite to `var=$((var + …))`. No behaviour change; removes the class from the file so the next `HINT_SEP=""` caller cannot resurrect it. |

## This round — the engine seam: one statement of what crosses it

Recurrence class, and the reason it leads: **the seam rows above are the same defect** — what
crosses the player/engine seam is restated per engine, with no single statement of the
convention. `normalize_playback_mode` is that restatement in code; §5/§6's remaining gaps are it
in prose (the `--auth` landing stated the convention for the cookie knob and nothing else).
Fixing the convention once prevents the family; fixing the rows individually does not. The two
doc rows open §5 once, so the small §5 omissions (`YT_AMBIG_WIDE`, `YT_DETACHED_LOG`, `-p`,
`--auth`) fold into the same edit rather than sitting in a backlog to open the file twice.

- [x] **R4 `shell/ut-play:260` + `shell/yt-resolve:280` + `shell/bili-resolve:273`** — state the canonical-mode rule in `AS-BUILT-contract.md` §3 (resolve surface) and §6 (checklist), then replace both resolvers' `normalize_playback_mode` with `validate_enum` over the five canonical values and delete the function from both. `ut-play` is untouched. done_when: `grep -l normalize_playback_mode shell/*` lists **only** `shell/ut-play` (which keeps its definition at `260` and its one call at `2291` — two occurrences, both staying); `shell/yt-resolve -f ba -j -- ID` exits 1 naming the five modes; `shell/yt-resolve -f audio -j -- ID` and `shell/ut-play -f ba -d -- ID` both still work; `tests/contract.sh --offline` green.
- [x] **R11 `docs/AS-BUILT-contract.md` §5 (one edit)** — list the six `BILI_*` knobs; fix `:642`'s format-row universality; add `YT_AMBIG_WIDE` (user) and `YT_DETACHED_LOG` (内部); add `-p` and `--auth` to the flag list. done_when: no line in §5 names a `YT_`-prefixed spelling as if every engine read it; and the knob-scoped sweep prints **nothing** — `for v in $(grep -ohE '\$\{(YT|UT|BILI)_[A-Z0-9_]+(:?[-=])' shell/* | grep -oE '(YT|UT|BILI)_[A-Z0-9_]+' | sort -u); do grep -q "$v" docs/AS-BUILT-contract.md || echo "MISSING $v"; done`. (Validated 2026-08-26: today it prints exactly the eight names this item adds — the six `BILI_*` knobs, `YT_AMBIG_WIDE`, `YT_DETACHED_LOG`. The first pass's unscoped `\b(YT|UT|BILI)_[A-Z0-9_]+` sweep was **unachievable**: it also caught plain internal globals — `BILI_REFERER`, `UT_HISTORY_LOOKED`, `UT_PLAY`, `UT_PLAYLIST`, `UT_VERSION`, `UT_HISTORY_BIN`, the R9 arrays — which no §5 should enumerate.)
- [x] **R11 `docs/AS-BUILT-contract.md:683`** — add the general prefix-convention item to §6 (engine tuning = `FOO_*`; `UT_*` suite-wide; `YT_*` legacy, not a template). done_when: §6 states it for an engine's *whole* knob family, not just the cookie knob its item 3 already covers; §5's per-engine rows point at it.
- [x] **R6/R8 `shell/ut-play:1928`** — *out of theme, in the round anyway:* the one correctness row in the inventory, a one-line fix, and leaving a path that changes mpv's volume then aborts with no envelope until the next audit is not a defensible trade. Add `\|\| true` — the posture the lock's own comment (`1164-1166`) and the four siblings already fixed. done_when: `grep -n 'lock_player_state\|lock_queue_state' shell/ut-play` shows the same failure handling at every call site, and the best-effort posture is stated once in `AS-BUILT-player.md` §9; `tests/playback.sh` green (it is the only suite that reaches a live socket).

## Deferred backlog

- **R9 naming drift: 5 arrays** — `YT_MPV_*_OPTIONS` (`ut-play:67,82,84`) + `YT_COOKIE_ARGS` (`yt-resolve:63`) + `BILI_COOKIE_ARGS` (`bili-resolve:71`): internal arrays wearing the env-knob prefix. Cheap renames; the resolver pair belongs with the R4 edit if that lands first, the player trio with a player-side change.
- **R1/R10: 1** — `C_YELLOW`, four writes / zero reads. Needs a decision (delete vs. stop tracking it as an exception), not a patch.
- **R5: 4** — the bare `((var += …))` statements in `shell/uting`. Latent only; a mechanical sweep, best done when `print_hints`/`wrap_print` are open for another reason.

## Not findings, recorded so the next audit does not re-open them

- **20 `DUP` rows in the function graph are the deliberate carve-outs**: `die` / `print_usage` / `require_cmd` / `require_deps` / `validate_enum` / `is_non_negative_int` / `ensure_scratch` / `cleanup_scratch` (each script must run standalone), and the whole engine-half family (`dump_once`, `emit_stream`, `format_for_mode`, `resolve_*`, `url_host`, `is_own_host`, `normalize_target`, `classify_yt_dlp_error`, `fetch_results`, `print_list`, `emit_search_json`, `reject_url`, `resolve_auth`) — separate executables whose alternative is the shared library the split exists to avoid. `normalize_playback_mode` is the **only** one of the 20 that spans player and engine, which is why it is the only one in the inventory.
- **`engine_resolve_bin` in `shell/ut-play:283` + `shell/uting:141`** — read both: the player's resolves one engine's binary for a play, the TUI's probes a candidate while **discovering** which engines are installed. Same name, different questions, neither on the other's hot path. Not merged, and the player must not gain a discovery loop.
- **`lock_player_state` has no stale-lock steal where `lock_playlist` does** (`ut-play:1167` vs `ut-playlist:180-205`) — deliberate and documented at `ut-playlist:160`: the player's writes are best-effort field patches, the store's can drop a track a user just added. The player's stale dirs are cleared by `rm_player_files` at reap and at both `--stop` paths. Left alone.
- **No path holds both `lock_player_state` and `lock_queue_state`** — checked all call sites (`405/420`, `1298/1305`, `1320/1328`, `1341/1353`, `1928/1934`). The hard rule at `ut-play:1208` holds.
- **`--auth` is landed and documented** (`51a9415`/`d7584de`): present in `AS-BUILT-contract.md` §1.3 (`:82-85`), §3 (envelope, `:295-312`), §5 (`:637-641`) and §6 (item 3, `:694`) — verified, no drift. The first pass's note defending its *absence* described a tree that no longer exists. The only `--auth` residue in this plan is what the landing did **not** state: the general prefix rule (§6 row above) and §5's missing `--auth` flag-list entry.
- **Plain internal globals wearing knob prefixes are not doc drift**: `UT_VERSION` (the version string each entry point reads), `UT_PLAY`/`UT_PLAYLIST`/`UT_HISTORY_BIN` (sibling-binary paths), `UT_HISTORY_LOOKED` (a memo flag), `BILI_REFERER` (a constant). None is an env read; §5 owes them nothing. The R9 arrays above are the same class with a worse costume, which is why they get renamed and these do not.
